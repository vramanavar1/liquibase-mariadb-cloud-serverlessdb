<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">
  <changeSet id="2" author="VivekRamanavar">
    <comment>Department: organizational units, plus DepartmentId foreign key on Employee.</comment>
    <sql>
      <![CDATA[
      CREATE TABLE `Department` (
          `DepartmentId`   INT          NOT NULL AUTO_INCREMENT,
          `DepartmentName` VARCHAR(128) NOT NULL,
          `Location`       VARCHAR(128) NULL,
          `IsActive`       TINYINT(1)   NOT NULL DEFAULT 1,
          CONSTRAINT `PK_Department` PRIMARY KEY (`DepartmentId`),
          CONSTRAINT `UQ_Department_DepartmentName` UNIQUE (`DepartmentName`)
      );

      ALTER TABLE `Employee` ADD COLUMN `DepartmentId` INT NULL;

      ALTER TABLE `Employee` ADD CONSTRAINT `FK_Employee_Department`
          FOREIGN KEY (`DepartmentId`) REFERENCES `Department` (`DepartmentId`);
      ]]>
    </sql>
    <rollback>
      <![CDATA[
      ALTER TABLE `Employee` DROP FOREIGN KEY `FK_Employee_Department`;
      ALTER TABLE `Employee` DROP COLUMN `DepartmentId`;
      DROP TABLE `Department`;
      ]]>
    </rollback>
  </changeSet>
</databaseChangeLog>
